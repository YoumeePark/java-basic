#pragma once

#include "CoreMinimal.h"

UENUM(BlueprintType)
enum class EImboItemType : uint8
{
	None = 0,
	Weapon = 1
};

UENUM(BlueprintType)
enum class EImboWeaponType : uint8
{
	None = 0,
	Fist = 1
};

